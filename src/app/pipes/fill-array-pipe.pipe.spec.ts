import { FillArrayPipePipe } from './fill-array-pipe.pipe';

describe('FillArrayPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FillArrayPipePipe();
    expect(pipe).toBeTruthy();
  });
});
